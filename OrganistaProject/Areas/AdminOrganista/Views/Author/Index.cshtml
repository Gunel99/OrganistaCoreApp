@model IEnumerable<Author>
@{
    ViewData["Title"] = "Index";
}

<div style="background: #1979a9; padding: 15px; color: #fff;">
    <h2 class="m-0">Author List</h2>
</div>

@if (Model.Count() > 0)
{
    <table class="table">
        <thead class="thead-light">
            <tr>
                <th>Id</th>
                <th>Image</th>
                <th>Fullname</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var author in Model)
            {
                <tr class="border-bottom:1px solid">
                    <td>@author.Id</td>
                    <td>
                        <img src="~/img/@author.Image" style="border-radius:0; width:160px; height:110px" />
                    </td>
                    <td style="font-family: auto; font-size: 18px;">@author.Fullname</td>
                    <td>
                        <form asp-route-id="@author.Id" asp-action="Delete" method="post">
                            <a class="btn btn-primary" asp-controller="Author" asp-action="Edit" asp-route-id="@author.Id">
                                <i class="far fa-edit"></i>
                            </a>
                            <button type="submit" class="btn btn-danger"><i class="fas fa-trash"></i></button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <div class="alert alert-danger">
        There are not any blogs.
    </div>
}
<a asp-area="AdminOrganista" asp-controller="Author" asp-action="Create" class="btn btn-success" style="margin: 30px 40px;">
    <i class="fas fa-plus" style="font-size: 15px; margin-right: 5px;"></i>
    Create Author
</a>



